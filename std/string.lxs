use crate::vec::Vec;

pub struct String {
    buf: Vec<u8>,
}

impl String {
    pub fn new() -> Self { String { buf: Vec::new() } }
    pub fn from_str(s: &str) -> Self { /* stub */ String::new() }
    pub fn push_str(&mut self, s: &str) { /* stub */ }
    pub fn len(&self) -> usize { self.buf.len() }
}
