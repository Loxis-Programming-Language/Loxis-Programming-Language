// Loixs — Python-style lowercase types

fun add(a: int, b: int): int {
    return a + b
}

fun greet(name: str) {
    print(name)
}

fun main(): void {
    var x: int = 42
    val pi: int = 3
    var y: int = add(x, pi)
    print(y)

    var name: str = "Loixs"
    greet(name)
}
