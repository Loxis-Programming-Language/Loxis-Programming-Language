fun fib(n: int) -> int {
    if n <= 1 {
        return n;
    };
    return fib(n - 1) + fib(n - 2);
}

fun main() -> int {
    var sum = 0;
    var i = 0;
    while i < 10 {
        sum = sum + i;
        i = i + 1;
    };
    return sum + fib(10);
}
