class Point {
    var x: int = 0
    var y: int = 0

    fun move(dx: int, dy: int) -> None {
        this.x += dx
        this.y += dy
    }
}

fun main() -> None {
    val p = Point_new()
    p.move(5, 3)
    print(p.x)
    print(p.y)
}
